apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ms-search-engine
  labels:
    app: {{ .Values.app.appMsSearchServer }}
spec:
  ingressClassName: {{ .Values.app.appMsSearchServer }}
  rules:
    - host: ms-search.server.local
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ .Values.app.appMsSearchServer }}
                port:
                  number: {{ .Values.service.port }}

